<% changes = YAML.load(review.versions.last.body) %>
<% changes.keys.each do |key| %>
  <br><br>"<%= t("review.for_message.#{key}") %>"
  <% if key != 'poster_url' %>
    <hr>
    <%= "Было:  #{changes[key][0]}" %>
    <hr>
    <%= "Стало: #{changes[key][1]}" %>
  <% end %>
  <br>
<% end %>
<br>
