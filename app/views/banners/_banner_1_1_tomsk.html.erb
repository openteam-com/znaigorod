<% if @placed_banner_first.present? %>
  <div class="placed_banner_wrapper placed_banner11">
    <%= link_to (image_tag @placed_banner_first.image_url, size: "1200x100"), @placed_banner_first.url, :target => @placed_banner_first.target, :class => "placed_banner #{@placed_banner_first.title}"%>
  </div>
<% end %>
